name: Tests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9']
    steps:
    - name: Checkout main branch
      uses: actions/checkout@v2
    - name: Unit tests
      run: |
        python3.9 -m unittest
